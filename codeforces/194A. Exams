#include <bits/stdc++.h>
using namespace std;
int dx[] = {-1, 0, 1, 0};
int dy[] = {0, 1, 0, -1}; 

int main() {
    
    int n, k;
    scanf("%d%d", &n, &k);
    if ( k / n > 2){
        puts("0");
        return 0;
    }
    int rem = k % n;
    printf("%d\n", n - rem);



    return 0;
}
