#include <iostream>
#include <stdio.h>
#include <string>
#include <string.h>
#include <vector>
#include <algorithm>
#include <memory.h>
#include <map>
#include <queue>
#include <stack>
#include <set>
#include <cmath>
#include <time.h>
#include <bitset>
#define ll long long 
#define f(i, x, n) for(int i = x; i < (int)(n); ++i)
using namespace std;
int main() {
  freopen ("in", "r", stdin);
  int t; 
  cin >> t; 
  while (t--){
    int n, f;
    cin >> n >> f;
    f(i, 0, f){
        f(j, 1, n +1){
            f(k, 0, j)
                cout << j;
            cout << endl;

        }
        for ( int j = n - 1 ; j >= 1; --j){
            f(k, 0, j)
                cout <<j;
            cout << endl;    
        }
            
            if (i + 1 < f)    
            cout << endl;
    }
    if (t)
        cout << endl;

  }
  
  return 0;
}
